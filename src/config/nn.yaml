model:
  hidden_dims: [128, 64, 32]
  dropout_rates: [0.2, 0.2, 0.1]
  use_batchnorm: [true, true, false]

training:
  epochs: 300
  batch_size: 64
  lr: 0.001
  weight_decay: 0.0001
  test_size: 0.2
  seed: 42
  grad_clip_norm: 1.0
  val_check_interval: 50
